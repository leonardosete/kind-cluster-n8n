apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ttsTranslator.name }}
  namespace: {{ .Values.ttsTranslator.namespace }}
  annotations:
    {{- with .Values.ttsTranslator.ingress.annotations }}
    {{ toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ttsTranslator.ingress.className }}
  rules:
    - host: {{ .Values.ttsTranslator.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ttsTranslator.name }}
                port:
                  number: {{ .Values.ttsTranslator.service.port }}
  tls:
    - hosts:
        - {{ .Values.ttsTranslator.ingress.host }}
      secretName: {{ .Values.ttsTranslator.ingress.tls.secretName }}
