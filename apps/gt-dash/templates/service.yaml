apiVersion: v1
kind: Service
metadata:
  name: gt-dash
  namespace: gt-dash
spec:
  selector:
    app: gt-dash
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}